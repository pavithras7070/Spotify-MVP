# Backend Port
PORT=5000

# Groq API Key (Get one for free at https://console.groq.com/keys)
# If left blank or 'mock', the system will use a smart mock response instead of hitting the API.
GROQ_API_KEY=your_groq_api_key_here
